'use strict'

module.exports = {
  buildRoutes: app => {
    //const routes = app.config.routes
    const compiledRoutes = []

    //TODO iterate to routes and compiled route in place and create native router on compiledRoutes

    return compiledRoutes
  }
}
